version: '3.8'

services:
  api:
    environment:
      - PGHOST=db
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ./server/db/scripts:/docker-entrypoint-initdb.d
